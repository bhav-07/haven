export const MAP_TILES = 120;
export const BOUNDARY_SIZE = 48;
export const MOVEMENT_SPEED = 3;
export const OFFSET = {
    x: -1900,
    y: -2700,
} as const;

export const SCREEN = {
    width: window.innerWidth,
    height: window.innerHeight,
} as const;

export const SPRITE_ANIMATION_FRAME_RATE = 15;